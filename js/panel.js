// Panel module placeholder (kept for future expansion).
export function initPanel(){
  // Currently the panel is driven by brain.js (title/desc updates).
  // Put future panel-specific logic here if needed.
}
